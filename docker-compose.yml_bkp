version: "2"

services:
  db:
    image: mongo:3.2
    ports:
      - 27017
  app:
    image: node
    links:
      - db
    volumes:
      - '.:/opt/'
    command: node /opt/server.js
    ports:
      - 3000:3000
